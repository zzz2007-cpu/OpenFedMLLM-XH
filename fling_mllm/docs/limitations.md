# Limitations

- The file `fling_mllm/flash_attn-2.7.3+cu12torch2.6cxx11abiFALSE-cp310-cp310-linux_x86_64.whl` is not included in version control.
- Install it manually if FlashAttention is required.
