你现在接手 OpenFedMLLM 仓库。请重新读取仓库根目录下的「科研目标.md」，注意：科研目标已经改变。现在论文主线不再是泛化的 OpenFedMLLM benchmark，而是 FedCHI 算法/分析论文。

最高优先级目标：
实现 FedCHI: Cross-Heterogeneity-Aware Federated LoRA Tuning for MLLMs under Label and Modality Heterogeneity。

论文核心问题：
在联邦 MLLM 微调中，标签异质性和模态异质性同时存在时，模型性能下降是否超过二者单独影响之和？如果存在这种超加性退化，能否通过异质性感知的 LoRA 分解与聚合缓解？

请先完整阅读：

- 科研目标.md
- 当前已有 FedCHI mock 实现
- fling_mllm/federated/
- fling_mllm/pipeline/
- mllmzoo/configs/
- scripts/
- dataset / collator / trainer 相关代码

注意：

1. 之前的 tiny/mock FedCHI 只能作为 smoke test。
2. 现在要实现正式论文主线。
3. 不要再追求“大而全 benchmark”，不要把 CrisisMMD、Hateful Memes、很多模型、很多任务全都铺开。
4. 主线优先 ScienceQA + Qwen2.5-VL-3B。
5. 如果 Qwen2.5-VL-3B 接入成本过高，可以先保留 MiniCPM-V 兼容接口，但正式主配置必须面向 ScienceQA + Qwen2.5-VL-3B。
6. 不要编造实验结果。
7. 不要伪实现 FedCHI。FedCHI 必须真的包含 shared LoRA、modality-specific LoRA、异质性感知聚合和 consistency loss 的可开关实现。

============================================================
一、正式实验主线
================

主数据集：
ScienceQA。

主模型：
Qwen2.5-VL-3B 或仓库中可实际接入的 Qwen2.5-VL 系列模型。

任务形式：
ScienceQA 多选题分类 / 生成式回答均可，但必须统一稳定。

要求：

1. question text 永远保留，因为它是任务指令。
2. 模态异质性不等于删除 question。
3. 模态异质性定义为额外上下文可用性差异：
   - image context 是否可用
   - text context / hint / lecture / explanation 是否可用
4. 不同客户端拥有不同 context modality。
5. evaluation 必须把模型输出解析为选项，并计算 accuracy / macro-F1。

============================================================
二、CHI 指标实现
================

请实现 Cross-Heterogeneity Interaction, CHI：

设：
A_00：无标签异质性、无模态异质性。
A_L0：只有标签异质性。
A_0M：只有模态异质性。
A_LM：标签异质性和模态异质性同时存在。

D_L = A_00 - A_L0
D_M = A_00 - A_0M
D_LM = A_00 - A_LM

CHI = D_LM - D_L - D_M
等价于：
CHI = A_L0 + A_0M - A_00 - A_LM

要求新增脚本：
scripts/analysis/compute_chi.py

功能：

1. 输入多个实验 output dir。
2. 读取 final_summary.json 或 eval_metrics.json。
3. 自动识别 setting：
   - A_00
   - A_L0
   - A_0M
   - A_LM
4. 输出：
   - chi_summary.csv
   - chi_summary.md
   - chi_by_algorithm.json
5. 至少支持按 algorithm 比较：
   - FedAvg-LoRA
   - FedProx-LoRA
   - FedCHI
   - FedCHI ablations
6. 报告：
   - A_00
   - A_L0
   - A_0M
   - A_LM
   - D_L
   - D_M
   - D_LM
   - CHI
   - CHI reduction relative to FedAvg-LoRA
7. 不允许缺失结果时报错崩溃，缺失项标记 MISSING。

FedCHI 的核心目标不是所有场景 accuracy 都最高，而是：
CHI_FedCHI < CHI_FedAvg_LoRA

请在 summary 中明确输出这一点。

============================================================
三、ScienceQA 联邦划分协议
==========================

新增或完善 ScienceQA 联邦划分脚本：

建议路径：
scripts/data/build_federated_scienceqa_chi.py

输入：

- ScienceQA 原始数据路径
- image root
- 输出目录
- num_clients
- alpha
- modality setting
- seed

输出目录结构建议：

data/scienceqa/federated_chi/
  L0_M0/
    client_0.jsonl
    ...
    meta.json
  L1_M0/
  L0_M1/
  L1_M1/
  L2_M2/
  ...

必须支持 2x2 最小矩阵：
S1: L0_M0，无标签异质性，无模态异质性
S2: L1_M0，有标签异质性，无模态异质性
S3: L0_M1，无标签异质性，有模态异质性
S4: L1_M1，有标签异质性，有模态异质性

也要支持 3x3 扩展矩阵：
Label heterogeneity:

- L0: IID 或 alpha=10
- L1: alpha=0.5 或 alpha=1.0
- L2: alpha=0.1

Modality heterogeneity:

- M0: 所有客户端 full context
- M1: 50% full, 25% text-context-only, 25% image-context-only
- M2: 20% full, 40% text-context-only, 40% image-context-only

ScienceQA 样本字段建议统一成：
{
  "id": "...",
  "question": "...",
  "choices": [...],
  "answer": 0,
  "subject": "...",
  "topic": "...",
  "category": "...",
  "skill": "...",
  "image": "...",
  "hint": "...",
  "lecture": "...",
  "solution": "...",
  "client_id": 0,
  "label_heterogeneity": "L1",
  "modality_heterogeneity": "M1",
  "available_context": {
    "question": true,
    "image": true/false,
    "text_context": true/false
  },
  "client_modality_type": "full/text_only/image_only"
}

标签异质性划分：
优先按 answer label 做 Dirichlet。
如果 ScienceQA answer label 类别太少，也支持按 subject/topic/category/skill 做 Dirichlet。
请把 split_target 配成参数：
--split_target answer
--split_target subject
--split_target topic
--split_target category
--split_target skill

meta.json 至少包含：

- dataset
- num_clients
- seed
- label_level: L0/L1/L2
- modality_level: M0/M1/M2
- alpha
- split_target
- modality_ratio
- 每个 client 样本数
- 每个 client label distribution
- 每个 client modality type
- 每个 client available_context distribution
- global label distribution
- global modality distribution

============================================================
四、Qwen2.5-VL + ScienceQA Dataset / Collator / Eval
====================================================

请实现正式 ScienceQA 数据读取、prompt 构造、collator 和 eval。

Dataset 需要支持：

1. full context：
   question + choices + image + text context
2. text-context-only：
   question + choices + text context，无 image
3. image-context-only：
   question + choices + image，无额外 text context
4. minimal question-only fallback：
   question + choices

Prompt 必须稳定，比如：

Question: {question}
Choices:
A. ...
B. ...
C. ...
D. ...
Context: {hint/lecture if available}
Please answer with the option letter only.

如果有 image，则走 Qwen2.5-VL 的图文输入。
如果无 image，则走 text-only 输入，但仍使用同一模型接口。

Eval：

1. 生成答案。
2. 解析 A/B/C/D 或 0/1/2/3。
3. 无法解析时记录 invalid prediction。
4. 输出：
   - accuracy
   - macro_f1
   - invalid_rate
   - per_class_f1
   - predictions.jsonl

============================================================
五、FedCHI 方法实现
===================

FedCHI 不能只是改聚合权重。请实现完整结构：

FedCHI = shared LoRA + modality-specific LoRA + heterogeneity-aware aggregation + consistency loss。

---

5.1 LoRA 分解
-------------

基础形式：
W' = W + ΔW_s + Σ_{m in M_k} ΔW_m

其中：
ΔW_s：shared LoRA，所有客户端都有。
ΔW_image：image-context LoRA，拥有 image context 的客户端激活。
ΔW_text：text-context LoRA，拥有 text context 的客户端激活。

客户端激活规则：

1. full client:
   shared LoRA + image LoRA + text LoRA
2. image-only client:
   shared LoRA + image LoRA
3. text-only client:
   shared LoRA + text LoRA
4. question-only fallback:
   shared LoRA only

要求：

1. 一次前向中可以同时激活 shared LoRA 和 modality-specific LoRA。
2. 不是训练两遍。
3. 反向传播时只更新被激活的 LoRA 参数。
4. 保存 checkpoint 时区分：
   - shared_adapter
   - image_adapter
   - text_adapter
5. 聚合时也区分三类 adapter。

实现方式可以选择：
A. 多 adapter PEFT：为同一模型注册 shared/image/text 三套 LoRA adapter，并在 forward 前动态启用组合。
B. 如果 PEFT 不支持多 adapter 同时激活，则实现自定义 LoRA wrapper，支持多个 LoRA delta 相加。
C. 如果完全实现复杂，请先实现最小可运行版，但必须真实区分参数命名空间：

- lora_shared.*
- lora_image.*
- lora_text.*

最终报告必须说明采用哪种方案。

---

5.2 LoRA target modules
-----------------------

默认建议：
shared LoRA：

- LLM attention q_proj/k_proj/v_proj/o_proj
- 或仓库已有 Qwen target modules

text-context LoRA：

- LLM attention / MLP selected layers

image-context LoRA：

- vision projector / multimodal connector / selected LLM layers
- 如果 Qwen2.5-VL connector 名称难定位，请写自动匹配逻辑，并在日志中打印实际命中的 target modules

要求：

1. target modules 可配置。
2. 启动时打印 shared/image/text 各自 target modules。
3. 如果某类 target modules 为空，必须抛出清晰错误，不要静默训练空参数。

---

5.3 本地损失函数
----------------

客户端 k 的本地训练：
L_k = L_task + lambda_cons * L_cons + mu_prox * L_prox

L_task:

- 生成式 CE loss 或分类 CE loss。

L_cons:
对于 full context 样本：

- 构造 x_full
- 随机 mask image 或 text context 得到 x_masked
- 要求输出分布接近。

可选距离：

- KL divergence between logits
- MSE between normalized logits
- CE consistency with same label

要求：

1. lambda_cons 可配置。
2. consistency loss 可关闭。
3. 只在 full context 样本上启用。
4. 如果 batch 内没有 full context 样本，L_cons = 0。
5. 日志记录 task_loss、cons_loss、prox_loss。

L_prox:

- 对 shared LoRA 参数计算 ||ΔW_s^k - ΔW_s^g||^2。
- mu_prox 可配置。
- 默认可关闭。

---

5.4 FedCHI 聚合
---------------

shared LoRA：
聚合所有客户端。

image LoRA：
只聚合拥有 image context 的客户端。

text LoRA：
只聚合拥有 text context 的客户端。

聚合公式：
raw_weight_k = n_k / div_k

div_k 可以先定义为：
div_k = 1 + lambda_label * label_div_k + lambda_modality * modality_div_k

其中：
label_div_k:

- 客户端 label distribution 与 global label distribution 的 L1 distance。

modality_div_k:

- 客户端 modality distribution 与 global modality distribution 的 L1 distance。

normalized_weight = raw_weight / sum(raw_weight)

要求：

1. lambda_label、lambda_modality 可配置。
2. 每轮保存：
   fedchi_weights.jsonl
3. 每条记录包含：
   - round
   - adapter_type: shared/image/text
   - client_id
   - n_k
   - label_div
   - modality_div
   - div_k
   - raw_weight
   - normalized_weight
   - client_modality_type
4. FedAvg-LoRA 仍按样本数聚合。
5. FedCHI 聚合逻辑必须和 FedAvg-LoRA 分开，不能污染 baseline。

============================================================
六、Baselines 与 Ablations
==========================

必须实现或配置以下 baseline：

基础：

1. Local-only
2. FedAvg-LoRA
3. FedProx-LoRA
4. SCAFFOLD-LoRA，如果仓库已有可接入
5. FedAdam/FedOpt，如果实现成本可控

FedCHI：

1. FedCHI full
2. w/o modality-specific LoRA
3. w/o shared LoRA
4. w/o heterogeneity-aware aggregation
5. w/o consistency loss
6. shared LoRA only

注意：

1. 论文最核心对比至少要有 FedAvg-LoRA、FedProx-LoRA、FedCHI full、FedCHI ablations。
2. 如果 SCAFFOLD/FedAdam 来不及完整实现，不要伪实现；保留配置并在报告中标注未验证。
3. FedCHI ablation 必须真实关闭对应模块。

============================================================
七、实验配置文件
================

新增正式配置文件，建议放在：
mllmzoo/configs/scienceqa_chi/

至少提供：

2x2 CHI 主实验：

1. scienceqa_qwen25vl_fedavg_lora_L0_M0.py
2. scienceqa_qwen25vl_fedavg_lora_L1_M0.py
3. scienceqa_qwen25vl_fedavg_lora_L0_M1.py
4. scienceqa_qwen25vl_fedavg_lora_L1_M1.py

FedCHI 对比：
5. scienceqa_qwen25vl_fedchi_L0_M0.py
6. scienceqa_qwen25vl_fedchi_L1_M0.py
7. scienceqa_qwen25vl_fedchi_L0_M1.py
8. scienceqa_qwen25vl_fedchi_L1_M1.py

强异质性扩展：
9. scienceqa_qwen25vl_fedavg_lora_L2_M2.py
10. scienceqa_qwen25vl_fedprox_lora_L2_M2.py
11. scienceqa_qwen25vl_fedchi_L2_M2.py

Ablation：
12. scienceqa_qwen25vl_fedchi_no_modality_lora_L1_M1.py
13. scienceqa_qwen25vl_fedchi_no_shared_lora_L1_M1.py
14. scienceqa_qwen25vl_fedchi_no_hetagg_L1_M1.py
15. scienceqa_qwen25vl_fedchi_no_consistency_L1_M1.py
16. scienceqa_qwen25vl_shared_lora_only_L1_M1.py

每个 config 必须包含：

- dataset root
- federated split path
- model path / HF model name
- output dir
- num_clients
- clients_per_round
- rounds
- local_epochs
- batch size
- gradient accumulation
- precision
- LoRA rank / alpha / dropout
- shared/image/text LoRA target modules
- fed algorithm
- lambda_label
- lambda_modality
- lambda_cons
- mu_prox
- seed
- max samples debug 参数
- eval_every

默认正式设置：

- num_clients = 10
- clients_per_round = 5
- rounds = 20
- local_epochs = 1
- batch_size = 1
- LoRA r = 8
- LoRA alpha = 16
- LoRA dropout = 0.05
- bf16 = true
- seeds = 3 个起步，推荐 3-5 个

============================================================
八、云集群脚本
==============

新增：

scripts/cloud/run_one_scienceqa_chi.sh
scripts/cloud/run_scienceqa_chi_2x2.sh
scripts/cloud/run_scienceqa_chi_l2m2.sh
scripts/cloud/run_scienceqa_chi_ablation.sh

可选：
scripts/slurm/run_one_scienceqa_chi.sbatch
scripts/slurm/run_scienceqa_chi_matrix.sbatch

要求：

1. 不写死用户名。
2. DATA_ROOT、MODEL_PATH、OUTPUT_ROOT 从环境变量读取。
3. 支持 CONFIG_NAME 参数。
4. 支持 SEED 参数。
5. 自动创建输出目录。
6. 保存 stdout/stderr。
7. 失败时 exit code 非 0。
8. 先提供 dry-run 模式。

推荐云集群执行顺序写进文档：

1. 准备 conda 环境。
2. 准备 ScienceQA 数据和图片。
3. 生成 federated CHI splits。
4. 跑单个 L0_M0 FedAvg-LoRA dry-run。
5. 跑 2x2 FedAvg-LoRA。
6. 跑 2x2 FedCHI。
7. 计算 CHI。
8. 跑 L2_M2 强异质性。
9. 跑 ablations。
10. 汇总结果。

============================================================
九、输出工件
============

每个实验 output dir 至少包含：

- config_resolved.json
- train_log.jsonl
- round_metrics.jsonl
- eval_metrics.json
- predictions.jsonl
- final_summary.json
- adapter checkpoints
- adapter_manifest.json

FedCHI 额外包含：

- fedchi_weights.jsonl
- adapter_type_metrics.json
- loss_breakdown.jsonl

final_summary.json 至少包含：

- dataset
- model
- algorithm
- label_level
- modality_level
- alpha
- modality_ratio
- seed
- final_accuracy
- final_macro_f1
- invalid_rate
- best_round
- best_accuracy
- best_macro_f1
- output_dir
- checkpoint_path

============================================================
十、验证要求
============

请至少执行：

1. 语法检查：
   python -m compileall .
2. help 检查：
   python scripts/data/build_federated_scienceqa_chi.py --help
   python scripts/analysis/compute_chi.py --help
3. config validate：
   不加载大模型，只检查 config、路径、字段、target modules 配置结构。
4. tiny/mock smoke：
   确保之前 mock FedCHI 没坏。
5. ScienceQA split smoke：
   用极小样本生成 L0_M0、L1_M0、L0_M1、L1_M1。
6. formal dry-run：
   如果本地没有 Qwen2.5-VL 权重，不要强行下载。
   允许 local_files_only=true。
   如果缺模型，输出清晰错误：
   “model weights not found; formal dry-run requires MODEL_PATH or HF access”
   不能出现难懂的 stack trace。

============================================================
十一、最终中文报告
==================

完成后请输出中文报告，必须包括：

1. 你重新读取科研目标.md 后确认的新论文主线。
2. 新主线与旧 benchmark 主线的区别。
3. 实际新增/修改的文件列表。
4. FedCHI 当前实现细节：
   - shared LoRA 在哪里实现
   - image LoRA 在哪里实现
   - text LoRA 在哪里实现
   - 多 LoRA 如何同时激活
   - consistency loss 在哪里实现
   - heterogeneity-aware aggregation 在哪里实现
   - CHI 指标在哪里计算
5. 已验证命令和 exit code。
6. 未验证部分和原因。
7. 云集群正式训练命令顺序。
8. 如果我要上传到云集群，最少需要准备哪些目录：
   - ScienceQA json/data
   - images
   - Qwen2.5-VL-3B model weights 或 HF access
   - output root
9. 明确指出哪些 baseline 是完整实现，哪些只是预留配置。
10. 不要说“应该可以”，只能说“已验证”或“未验证”。

实现优先级：
P0：ScienceQA federated CHI split + CHI metric script。
P1：Qwen2.5-VL ScienceQA dataset/collator/eval。
P2：FedAvg-LoRA 正式联邦训练跑通。
P3：FedCHI shared/image/text LoRA 分解与聚合。
P4：consistency loss。
P5：FedProx-LoRA。
P6：ablation configs。
P7：SCAFFOLD/FedAdam 等扩展 baseline。

如果时间不够，必须先保证 P0-P3 真实完成，而不是平均用力写一堆跑不通的 baseline。
